import 'package:flutter/material.dart';

import 'base_screen.dart';

class DataProvidersScreen extends StatelessWidget {
  const DataProvidersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const BaseLegalScreen(
      title: 'Data Providers',
      children: [],
    );
  }
}
